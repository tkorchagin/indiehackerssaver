# 🎨 Настройка генерации мокапов

## Как настроить

Все настройки находятся в файле **`mockup_config.py`**

### Параметры:

#### 1. `MAX_IMAGE_SIZE` (по умолчанию: 1024)
Максимальный размер самой длинной стороны изображения в пикселях.

**Примеры:**
```python
MAX_IMAGE_SIZE = 1024  # Для Instagram (оптимально)
MAX_IMAGE_SIZE = 1920  # Для больших экранов
MAX_IMAGE_SIZE = 800   # Для быстрой генерации
```

#### 2. `MOCKUP_PADDING` (по умолчанию: 80)
Отступы вокруг изображения в пикселях (внутри белого окна).

**Примеры:**
```python
MOCKUP_PADDING = 80   # Стандартные отступы
MOCKUP_PADDING = 120  # Большие отступы (более воздушно)
MOCKUP_PADDING = 50   # Минимальные отступы
```

#### 3. `BACKGROUND_IMAGE` (по умолчанию: None)
Путь к изображению для фона. Если `None` - используется градиент по умолчанию.

**Примеры:**
```python
BACKGROUND_IMAGE = None  # Градиент по умолчанию
BACKGROUND_IMAGE = "backgrounds/sunset.png"  # Кастомный фон
BACKGROUND_IMAGE = "backgrounds/dark_gradient.jpg"  # Темный градиент
```

---

## Как добавить свой фон

### Шаг 1: Создайте или скачайте изображение
- Минимальный размер: **1920x1080px**
- Форматы: PNG, JPG, JPEG
- Рекомендуется: градиенты, абстрактные фоны

### Шаг 2: Поместите изображение в папку `backgrounds/`
```bash
cp your-background.png backgrounds/
```

### Шаг 3: Обновите `mockup_config.py`
```python
BACKGROUND_IMAGE = "backgrounds/your-background.png"
```

### Шаг 4: Перезапустите бота
```bash
# Остановите бота (Ctrl+C)
# Запустите заново
source venv/bin/activate
python post_maker_bot.py
```

---

## Готовые градиенты

Вы можете создать красивые градиенты на этих сайтах:

### Создание градиентов:
- [CSS Gradient](https://cssgradient.io/) - создать и скачать градиент
- [UI Gradients](https://uigradients.com/) - готовые градиенты
- [Coolors Gradients](https://coolors.co/gradients) - коллекция градиентов

### Скачать готовые фоны:
- [Unsplash](https://unsplash.com/s/photos/gradient) - бесплатные фото
- [Pexels](https://www.pexels.com/search/gradient/) - градиентные фоны
- [Gradienta](https://gradienta.io/) - коллекция градиентов

---

## Примеры настроек

### Для Instagram Stories:
```python
MAX_IMAGE_SIZE = 1080
MOCKUP_PADDING = 100
BACKGROUND_IMAGE = "backgrounds/gradient1.png"
```

### Для больших постов:
```python
MAX_IMAGE_SIZE = 1920
MOCKUP_PADDING = 150
BACKGROUND_IMAGE = None  # Градиент по умолчанию
```

### Минималистичный стиль:
```python
MAX_IMAGE_SIZE = 800
MOCKUP_PADDING = 50
BACKGROUND_IMAGE = "backgrounds/minimal_white.png"
```

---

## Как работает масштабирование

Бот автоматически:
1. Проверяет размер входного изображения
2. Если какая-то сторона больше `MAX_IMAGE_SIZE`:
   - Уменьшает изображение пропорционально
   - Сохраняет соотношение сторон
3. Добавляет отступы вокруг изображения
4. Создает итоговый мокап

**Пример:**
- Входное изображение: 2000x1500px
- MAX_IMAGE_SIZE = 1024
- Результат: 1024x768px (внутри окна)
- Итоговый мокап: ~1284x948px (с отступами и рамкой)

---

## FAQ

**Q: Можно ли использовать разные фоны для разных фото?**
A: Сейчас нет, но можно менять `BACKGROUND_IMAGE` в конфиге и перезапускать бота.

**Q: Почему итоговое изображение больше MAX_IMAGE_SIZE?**
A: MAX_IMAGE_SIZE - это размер самого скриншота внутри окна. Итоговый мокап включает отступы, рамку окна и точки macOS, поэтому он больше.

**Q: Можно ли убрать точки macOS?**
A: Да, можно отредактировать функцию `screenshot_mockup()` в `post_maker_bot.py` и удалить блок `.dots`.

**Q: Какой формат лучше для фона?**
A: PNG для качества, JPG для меньшего размера файла. Оба работают отлично.

---

## Тестирование

Чтобы протестировать настройки без отправки в Telegram:

```bash
source venv/bin/activate
python test_new_mockup.py
```

Это создаст тестовое изображение и применит мокап с текущими настройками.
